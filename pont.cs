/*public delegate void MeuDelegate(string x);

*/

/*
int[] Lista = new int[]
{1,2,3,4,5};

int[] Valor = Transforme(Lista, n => n * n); //valor recebe a função Transforme
*/

/*
foreach(int i in Transforme(Lista, n => n * n)){  outro metodo de fazer, mas ele não muda a lista.
    Console.WriteLine(i);
};
*/

/*
foreach(int i in Valor){ 
    Console.WriteLine(i);
};

int[] Transforme(int[] entrada, Transformador<int> t)
{
    int[] List = new int[entrada.Length];
    for(int i = 0; i < entrada.Length; i++)
    {
        List[i] = t(entrada[i]);
    }
    return List;
}
*/

